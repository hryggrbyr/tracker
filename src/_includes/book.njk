---
layout: base.njk
---

<style>
.grid {
  display: grid;
  grid-template-columns: auto auto; /* Adjust based on number of tabs */
  grid-template-rows: auto 1fr;
}

.item {
	display: contents;
}

/* Make summaries look like tabs */
.item summary {
  grid-row: 1;
  cursor: pointer;
  padding: 0.5rem 1rem;
  list-style: none; /* Hide marker */
  border: 1px solid #ccc;
  border-bottom: none;
}

/* Hide the default marker in all browsers */
.item summary::-webkit-details-marker {
  display: none;
}

/* Position each tab based on --n variable */
.item[style*="--n: 1"] summary {
  grid-column: 1;
}

.item[style*="--n: 2"] summary {
  grid-column: 2;
}

/* Active tab styling */
.item[open] summary {
  border-bottom: 1px solid white;
  position: relative;
  z-index: 1;
}

/* All content areas occupy the same grid cell */
.details-content {
	max-width: 100%;
	overflow-x: auto;
  grid-row: 2;
  grid-column: 1 / -1; /* Span all columns */
  padding: 1rem;
  border: 1px solid #ccc;
}

/* Hide content when details is not open */
.item:not([open]) .details-content {
  display: none;
}

/* Show content only for open details */
.item[open] .details-content {
  display: block;
}
</style>

<h1>{{ title }} ({{ subtitle }}) by {{ author }}</h1>

<div class="grid">
  <!-- First tab: set to open -->
  <details class="item" name="alpha" open style="--n: 1;">
    <summary class="subitem">Meta Data</summary>
    <div class="details-content">
      <div class="card">
        <figure class="card-image"><img src="{{ coverUrl }}"></figure>
        <div class="card-title flow">
          <p class="title"><strong>{{ title }}</strong></p>
          {% if subtitle %}<p class="card-subtitle"></p>{% endif %}
          <p>{{ author }}</p>
        </div>
        <div class="card-author">
          {{ rating }}/5 @ {{ finished_date }}
        </div>
      </div>
    </div>
  </details>

  <details class="item" name="alpha" style="--n: 2;">
    <summary class="subitem">Review YAML</summary>
    <div class="details-content">
      <h2>YAML for Review</h2>
      <pre>
---
public: false
title: "Book Review — {{ title }} by {{ author }}"
date:
tags:
  - entertainment
  - books
  - review
{% for tag in tags %}  - {{ tag }}
{% endfor %}
description: "One of my sporadic book reviews"
layout: "book-review"
hero: "/images/book-review.jpg"
teaserImage: true
book:
  title: "{{ title }}"
  author: "{{ author }}"
  publishedDate: "{{ published }}"
  rating: "{{ rating or '0'}}"
  url: "{{ bookshopUrl or 'https://www.ebooks.com/en-gb/searchapp/searchresults.net?term=' ~ (isbn or (title | urlencode ~ '%20' ~ author | urlencode)) }}"
---
      </pre>
    </div>
  </details>
</div>